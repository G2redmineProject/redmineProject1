<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yedam.app.issuemodal.mapper.IssueModalMapper">
  <select id="selectIssueModalList" resultType="IssueModalVO">
    SELECT
      i.issue_code,
      i.project_code,
      i.title,
      u.name
    FROM issues i
    LEFT JOIN users u
      ON u.user_code = i.assignee_code
    WHERE i.project_code = #{projectCode}
    ORDER BY i.issue_code
  </select>
</mapper>